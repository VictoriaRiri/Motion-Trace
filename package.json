{
  "name": "motiontrace-mvp",
  "version": "0.2.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "dev:frontend": "npm --prefix frontend run dev"
  }
}
